[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: exercises Playwright against local fixture pages
    live: touches the real Amazon site (never places orders); skipped by default
filterwarnings =
    error::SyntaxWarning
